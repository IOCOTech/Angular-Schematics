{
    "$schema": "http://json-schema.org/schema",
    "$id": "ioco-service",
    "title": "New service in the ioco structure",
    "properties": {
        "name": {
            "description": "The name of the component",
            "type": "string",
            "x-prompt": "What is the name of the service"
        }
    },
    "required": ["name"]
}